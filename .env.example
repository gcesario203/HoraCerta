# Copie para .env na raiz: cp .env.example .env

# PostgreSQL (docker compose)
POSTGRES_USER=horacerta
POSTGRES_PASSWORD=horacerta
POSTGRES_DB=horacerta

# API
JWT_KEY=HoraCerta-Docker-Secret-Key-Min-32-Chars!!

# Web (build-time e runtime no compose)
API_URL=http://api:8080
